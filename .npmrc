# Storybook's peer ranges for Angular suelen ir un poco desfasadas de la versión
# exacta de Angular que usamos (20.3.x). legacy-peer-deps evita falsos conflictos
# de resolución sin afectar las versiones reales que se instalan.
legacy-peer-deps=true
